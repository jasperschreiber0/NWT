# NOTE: dashboard/app.py does NOT load this file — it hardcodes
# load_dotenv("/home/northworld/trading/nwt_agents/.env"), so the dashboard
# shares nwt_agents/.env in practice. This file exists only as a reference
# for what the dashboard process needs to find in that env file.
NWT_DB_DSN=postgresql://user:pass@localhost/nwt_agents
NWT_DASHBOARD_TOKEN=change_me_to_a_long_random_token
